# Python dependencies for the ytx package. Install into a project-local venv:
#   python -m venv .venv
#   .venv/Scripts/python -m pip install -r requirements.txt
# See docs/Setup.md for the full toolchain (deno + bgutil server + cookies).

yt-dlp
bgutil-ytdlp-pot-provider
